/*
** EPITECH PROJECT, 2024
** myteams
** File description:
** get_thread_by_uuid
*/

#include "myteams_server.h"

// thread_t *get_all_channel_by_uuid(struct teamhead *team_head, char *uuid)
// {
//     team_t *team = NULL;
//     thread_t *channel = NULL;

//     TAILQ_FOREACH(team, team_head, next) {
//         channel = get_channel_by_uuid(&team->channels_head, uuid);
//         if (strcmp(channel->channel_uuid, uuid) == 0)
//             return channel;
//     }
//     return NULL;
// }

// thread_t *get_thread_by_uuid(struct threadhead* thread_head, char *uuid)
// {
//     thread_t * = NULL;

//     TAILQ_FOREACH(channel, channel_head, next) {
//         if (strcmp(channel->channel_uuid, uuid) == 0)
//             return channel;
//     }
//     return NULL;
// }
